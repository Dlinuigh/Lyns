<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lynx">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".uclass.statement.StatementActivity"></activity>
        <activity android:name=".uclass.programming.ProgramActivity" />
        <activity android:name=".uclass.mutiplechoice.ChoiceActivity" />
        <activity android:name=".navigation.report.ReportActivity" />
        <activity android:name=".navigation.about.AboutActivity" />
        <activity android:name=".navigation.feedback.FeedbackActivity" />
        <activity android:name=".uclass.item.ItemActivity" />
        <activity
            android:name=".navigation.settings.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
